table:
  name: bank
  schema: public
array_relationships:
  - name: accounts
    using:
      foreign_key_constraint_on:
        column: bank_id
        table:
          name: account
          schema: public
