table:
  name: media
  schema: public
object_relationships:
  - name: dragon_user
    using:
      foreign_key_constraint_on: user_id
array_relationships:
  - name: clients
    using:
      foreign_key_constraint_on:
        column: logo_id
        table:
          name: client
          schema: public
