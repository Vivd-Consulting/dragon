table:
  name: gic
  schema: accounting
array_relationships:
  - name: transactions
    using:
      foreign_key_constraint_on:
        column: gic_id
        table:
          name: transactions
          schema: accounting
