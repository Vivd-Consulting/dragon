table:
  name: personal_finance_category
  schema: accounting
array_relationships:
  - name: transactions
    using:
      foreign_key_constraint_on:
        column: category_id
        table:
          name: transactions
          schema: accounting
