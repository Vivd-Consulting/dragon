table:
  name: transactions_recommendations
  schema: accounting
object_relationships:
  - name: recommendation
    using:
      manual_configuration:
        column_mapping:
          recommended_account_id: account_id
          recommended_transaction_id: id
        insertion_order: null
        remote_table:
          name: transactions
          schema: accounting
  - name: transaction
    using:
      manual_configuration:
        column_mapping:
          account_id: account_id
          transaction_id: id
        insertion_order: null
        remote_table:
          name: transactions
          schema: accounting
