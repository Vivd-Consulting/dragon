table:
  name: category
  schema: accounting
array_relationships:
  - name: transactions
    using:
      foreign_key_constraint_on:
        column: gic_category_id
        table:
          name: transactions
          schema: accounting
