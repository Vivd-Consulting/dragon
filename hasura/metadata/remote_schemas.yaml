- name: API
  definition:
    url: '{{REMOTE_SCHEMA}}'
    timeout_seconds: 60
    customization: {}
    headers:
      - name: action-secret
        value_from_env: ACTION_SECRET
    forward_client_headers: true
  comment: Internal Apollo
