query transaction($id: String!) {
  accounting_transactions_by_pk(id: $id) {
    id
    name
    date
    credit
    debit
    recommendations(where: {recommendation: {gic_category_id: {_is_null: true}}}) {
      recommendation {
        id
        name
        date
        credit
        debit
      }
    }
  }
}
