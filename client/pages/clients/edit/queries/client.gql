query client($id: Int!) {
  client(where: { id: { _eq: $id } }) {
    id
    contract_id
    logo_id
    name
    description
    gpt_persona
    start_date
    end_date
    created_at
    archived_at
  }
}
