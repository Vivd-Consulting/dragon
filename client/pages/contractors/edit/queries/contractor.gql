query contractor($id: Int!) {
  contractor(where: { id: { _eq: $id } }) {
    id
    name
    location
    contractor_rate {
      id
      rate
    }
    gpt_persona
    start_date
    end_date
  }
}
